{
  "installCommand": "pnpm install",
  "buildCommand": "cd artifacts/flo && pnpm run build",
  "outputDirectory": "artifacts/flo/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
